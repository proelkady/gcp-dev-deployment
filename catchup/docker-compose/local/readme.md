# Run local instructions

- Setup hosts file

```bash
sudo nano /etc/hosts
```

- Add below site mappings

```bash
127.0.0.1 catchup-app.app
127.0.0.1 auth-dev.catchup-app.app
127.0.0.1 api-dev.catchup-app.app
127.0.0.1 partner-dev.catchup-app.app
```
